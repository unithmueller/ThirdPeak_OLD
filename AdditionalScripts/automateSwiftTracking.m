function automateSwiftTracking(LocalisationDataPath, expDispla, pBleach, ChangeMin, IterMax, Count, parameterPath)
%Recursively call the swift tracking on the system to generate the correct
%tracked files
%


%Create a save location

%Prepare the settings file

%Call the external swift function

%Sort the new files into the new save location

%Estimate the new properties

%Calculate the change 

%Return the data

end